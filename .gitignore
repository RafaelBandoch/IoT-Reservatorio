# Credenciais reais — versionar só o .env.example
.env

# Artefatos da compilação local (regeráveis com ./compilar.sh)
build/

# Credenciais reais do firmware — copie de credenciais.exemplo.h
credenciais.h

# Cópia local do painel com as credenciais reais do cluster
painel-reservatorio.local.html

# Senha real do Postgres — gere a partir do .env (ver README)
pgadmin/pgpass

# Enunciados e materiais da disciplina
*.pdf
